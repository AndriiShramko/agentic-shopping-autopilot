# cli/ — planned
- `install` — lays out skills/ into ~/.claude/skills and ~/.agents/skills (Claude Code + Codex);
  compatible with `npx skills add`.
- `skill record <url>` — records a purchase → generalizes it into a site-skill (v1).
- `skill verify <site>` — runs the smoke tests, updates last_verified.
Empty for now: the MVP is installed by copying the skills/ folder (see the repo README).
