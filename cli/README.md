# cli/ — планируется
- `install` — раскладка skills/ в ~/.claude/skills и ~/.agents/skills (Claude Code + Codex);
  совместимость с `npx skills add`.
- `skill record <url>` — запись покупки → генерализация в site-skill (v1).
- `skill verify <site>` — прогон smoke-тестов, обновление last_verified.
Пока пусто: MVP ставится копированием папки skills/ (см. README репо).
